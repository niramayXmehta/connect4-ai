// Print a formatted table of training metrics to the terminal.

/**
 * Print a generation-summary row.
 * @param {object} stats
 * @param {number} stats.generation
 * @param {number} stats.bestFitness
 * @param {number} stats.avgFitness
 * @param {number} stats.gamesPlayed
 */
export function printGenStats(stats) {
  // TODO: implement pretty-printed table row
}

/** Print the column header row. */
export function printHeader() {
  // TODO: implement
}
