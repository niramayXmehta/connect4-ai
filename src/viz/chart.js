// ASCII chart of a metric over time (e.g. win rate per generation).

/**
 * Print an ASCII line chart to stdout.
 * @param {number[]} values   - one value per x-tick
 * @param {object}   opts
 * @param {string}   opts.title
 * @param {number}   opts.height  - chart rows (default 10)
 * @param {number}   opts.width   - chart columns (default 60)
 */
export function printChart(values, { title = '', height = 10, width = 60 } = {}) {
  // TODO: implement
}
